language: node_js
node_js:
    - "9"
    - "8"
    - "7"
    - "6"
after_success:
  - |
    if [ "$TRAVIS_PULL_REQUEST" == "false" ] && [ "$TRAVIS_BRANCH" == "master" ]; then
      chmod +x release.sh
      ./release.sh
    else
      echo "Not PR merge trigger"
    fi